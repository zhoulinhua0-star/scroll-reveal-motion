{
  "name": "scroll-reveal-motion-repository",
  "version": "1.0.0",
  "private": true,
  "description": "Validation tooling for the scroll-reveal-motion Agent Skill",
  "scripts": {
    "check:react": "npx --yes esbuild@0.25.9 skills/scroll-reveal-motion/assets/react/scroll-reveal.tsx --bundle --format=esm --platform=browser --external:react --outfile=.tmp/scroll-reveal-motion-react.js",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-skill.mjs && node --check skills/scroll-reveal-motion/assets/vanilla/scroll-reveal.js && npm run check:react && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
